/*
Pick a hardcoded secret number,
loop on `scanf` until the user guesses it,
print higher/lower hints.
*/

#include <stdio.h>

int main(void) {
	//get random seed, init num

	while(guess != num) {
		//get guess

		//print hint or gz
	}
	return 0;
}
